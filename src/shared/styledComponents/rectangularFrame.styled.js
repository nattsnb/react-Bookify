import styled from "styled-components";
import { Toolbar } from "@mui/material";

export const RectangularFrame = styled.div`
  background-color: var(--backgroung-gray);
  box-shadow: 0 0 3px var(--secondary-light);
`;
